项目lua行数统计：
^(?!([\t ]*$)|([^\t ]*--)).+$
---
^ 匹配行开头
(?!([\t ]*$)|([^\t ]*--)) 该部分是一个负向前置断言，包含两个条件：
([\t ]*$) 匹配 0 或多个空格或制表符，然后匹配行结尾。该条件的作用是避免匹配只包含空格和制表符的行。
([^\t ]*--) 匹配除了制表符和空格以外的任意字符，然后匹配一个连字符。该条件的作用是避免匹配单行注释。
.+ 匹配至少一个非换行字符，也就是匹配非空行
$ 匹配行结尾
---
